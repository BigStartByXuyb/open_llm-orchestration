"""
Gateway — HTTP/WebSocket 入口层（FastAPI）
Gateway — HTTP/WebSocket entry layer (FastAPI).

Layer 1: Only imports from shared/ (and wiring/ via dependency injection).
第 1 层：仅从 shared/ 导入（通过依赖注入访问 wiring/ 层）。
"""
